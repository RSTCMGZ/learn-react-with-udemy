export type instructorProps = {
    instructorNameLastName: {
        firstName: string
        lastName: string
    }
}


export type DateComing = {
    name: string;
    age: number;
    courseNumber: number;
    isBest: boolean;
}